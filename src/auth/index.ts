/**
 * src/auth — accounts (NOT YET IMPLEMENTED).
 *
 * GUARDRAIL: changes to auth handle PII (G4) and require human approval
 * (CLAUDE.md). Never log PII or secrets. Document lawful basis before
 * collecting new user data. Intentionally a stub pending sign-off.
 */

export {};
